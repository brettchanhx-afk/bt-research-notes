# -*- coding: utf-8 -*-
"""
基金项目：基金业绩持续性量化评价
===================================
研报复现：华泰金工研报《定量评价基金的业绩持续性》

作者：AI量化工程师
日期：2026-04-28
"""

import os

# ============================================================
# 路径配置
# ============================================================
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
DATA_DIR = os.path.join(BASE_DIR, "data")
OUTPUT_DIR = os.path.join(BASE_DIR, "output")
SOURCE_DIR = os.path.join(BASE_DIR, "source")

# 确保目录存在
for _dir in [DATA_DIR, OUTPUT_DIR]:
    os.makedirs(_dir, exist_ok=True)

# ============================================================
# 数据源优先级配置（按用户指定顺序）
# ============================================================
# 第一优先级：免费开源Python库
DATA_SOURCE_PRIORITY = [
    "efinance",     # 基金净值、持仓、股票行情等
    "akshare",      # A股、期货、基金、宏观经济、大宗商品等
    "baostock",     # A股历史行情、指数、财务数据
    "mootdx",       # 通达信数据接口
    "yfinance",     # 美股、国际期货、外汇
    "bondpy",       # 债券数据
    "fundata",      # 基金数据
    "ifind",        # 同花顺iFind MCP接口
]
# 第二优先级：最后选项
FALLBACK_DATA_SOURCE = "neodata-financial-search"

# ============================================================
# 业绩持续性分析配置
# ============================================================
# 无风险利率（年化，用于计算超额收益）
RISK_FREE_RATE = 0.03  # 3%年化无风险利率

# 横截面分析法配置
CROSS_SECTION_CONFIG = {
    "min_periods": 20,          # 最少需要的历史期数
    "confidence_threshold": 0.05,  # 显著性水平
}

# 交叉积比率法配置
CPR_CONFIG = {
    "min_periods": 4,          # 最少需要4个期间（形成3对相邻期间）
    "require_nonzero": True,    # 要求WL和LW均不为0
}

# Hurst指数法配置
HURST_CONFIG = {
    "min_periods": 32,         # 最少需要32期数据
    "n_values": [4, 8, 16, 32],  # 子集长度候选值
    "n_estimators": 8,         # 每个n值估计次数取平均
}

# ============================================================
# 回测配置
# ============================================================
BACKTEST_CONFIG = {
    "rebalance_freq": "quarterly",  # 调仓频率：季度
    "top_n": 10,                     # 选取前N只基金
    "persistence_threshold": 0.6,    # 持续性阈值（仅选持续性强的基金）
}

# ============================================================
# 可视化配置
# ============================================================
PLOT_CONFIG = {
    "figsize": (12, 8),
    "dpi": 120,
    "style": "seaborn-v0_8-whitegrid",
    "chinese_font": ["Microsoft YaHei", "SimHei", "DejaVu Sans"],
    "date_format": "%Y-%m-%d",
}

# ============================================================
# 基金池配置
# ============================================================
# 默认基金池：偏股混合型基金（可根据需要修改）
DEFAULT_FUND_POOL = [
    # 示例基金代码，可替换为实际基金
    "000628",  # 大成高鑫股票A
    "008404",  # 华泰紫金泰盈混合A
    "021181",  # 中欧价值精选混合A
    "000012",  # 华安纯债债券A
    "024106",  # 万家裕利债券A
]

# ============================================================
# 输出格式配置
# ============================================================
OUTPUT_CONFIG = {
    "save_csv": True,
    "save_json": True,
    "save_plot": True,
    "plot_format": "png",
}
